# 23. **Exercício 23:** Use `map` com uma função lambda para calcular a média dos
# valores de uma lista de tuplas.
from statistics import mean

lista = [10, 8, 10, 9]
media = mean(lista)

print(media)
